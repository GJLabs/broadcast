import {
  StyleSheet,
  Dimensions,
} from 'react-native';

// marginBottom allows space for the lower navbar
const styles = StyleSheet.create({
  container: {
    position:'relative',
    marginBottom: 37.5
  }
})

module.exports = styles;